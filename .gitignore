# Note: android/app/google-services.json IS committed — it's Firebase client config
# (package-restricted API key, already ships in every APK), not a secret. The real secret
# (the FCM service-account key) lives only in the server's gitignored web.env.

# Bundled background videos are downloaded during CI, not stored in the repo.
WorldCupDuck/*.mp4
android/app/src/main/assets/*.mp4

# Build output
build/
.gradle/
*.ipa
*.apk
Payload/
